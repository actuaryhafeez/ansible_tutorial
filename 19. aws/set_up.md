### Controller manager set up:
    
    sudo yum clean all
    sudo yum list updates
    sudo yum makecache
    
    sudo yum install epel-release
    sudo yum install ansible
    yum install python-pip
    pip install --upgrade pip
    pip install boto
    
    sudo yum makecache

